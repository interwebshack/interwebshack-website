name: Deploy static site to Cloudflare R2 (apex + www)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      # --- Required Secrets (set in GitHub: Settings → Secrets and variables → Actions) ---
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN:  ${{ secrets.CLOUDFLARE_API_TOKEN }}   # must allow: R2 Storage Write, DNS Write, Zone Transform/Origin Rules Write
      R2_ACCESS_KEY_ID:      ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY:  ${{ secrets.R2_SECRET_ACCESS_KEY }}
      R2_BUCKET:             ${{ secrets.R2_BUCKET }}              # e.g., site-interwebshack-com-<hash>
      R2_CUSTOM_DOMAIN:      ${{ secrets.R2_CUSTOM_DOMAIN }}       # e.g., static.interwebshack.com

      # --- Derived / Configurable ---
      ZONE_NAME: interwebshack.com
      SITE_DIR: site
      INDEX_OBJECT: index.html
      R2_ENDPOINT: https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.r2.cloudflarestorage.com

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # If you have a static-site build step, run it and output to $SITE_DIR.
      # - name: Build site
      #   run: <build> && mv dist "$SITE_DIR"

      - name: Sanity check local site folder
        shell: bash
        run: |
          set -euo pipefail

          echo "🔍 Verifying site build output exists"
          echo "📁 SITE_DIR       = $SITE_DIR"
          echo "📄 INDEX_OBJECT   = $INDEX_OBJECT"

          if [[ ! -d "$SITE_DIR" ]]; then
            echo "❌ Expected directory ./$SITE_DIR but it does not exist."
            echo "   This usually means your build step didn't output to the correct folder."
            exit 1
          fi


          if [[ ! -f "$SITE_DIR/$INDEX_OBJECT" ]]; then
            echo "❌ Expected file ./$SITE_DIR/$INDEX_OBJECT but it was not found."
            echo "   Check your build command and output path."
            exit 1
          fi

          echo "✅ Site directory and entry file verified"

      # --- Get zone_id for interwebshack.com ---
      - name: Resolve Cloudflare zone_id
        id: zone
        shell: bash
        run: |
          set -euo pipefail

          echo "🔍 Fetching Cloudflare Zone ID for: $ZONE_NAME"

          # Make API request
          response=$(curl -sS -w "\n%{http_code}" \
            -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            "https://api.cloudflare.com/client/v4/zones?name=${ZONE_NAME}")

          # Split body and HTTP status
          body=$(echo "$response" | sed '$d')
          http_status=$(echo "$response" | tail -n1)

          echo "📡 Cloudflare API HTTP status: $http_status"

          # If Cloudflare returned error
          if [[ "$http_status" -ne 200 ]]; then
            echo "❌ Error: Cloudflare API returned HTTP $http_status"
            echo "Response body:"
            echo "$body" | jq .
            exit 1
          fi

          success=$(echo "$body" | jq -r '.success')
          if [[ "$success" != "true" ]]; then
            echo "❌ Cloudflare returned success=false"
            echo "$body" | jq .
            exit 1
          fi

          zone_count=$(echo "$body" | jq '.result | length')
          if [[ "$zone_count" -eq 0 ]]; then
            echo "❌ No zones found matching: $ZONE_NAME"
            exit 1
          fi

          zone_id=$(echo "$body" | jq -r '.result[0].id')
          zone_name=$(echo "$body" | jq -r '.result[0].name')

          echo "✅ Found zone: $zone_name"
          echo "🔑 Zone ID: $zone_id"

          echo "zone_id=$zone_id" >> "$GITHUB_OUTPUT"

        # Docs: List Zones (get zone_id). 
        # https://developers.cloudflare.com/api/resources/zones/methods/list/

      # --- Ensure R2 bucket exists ---
      - name: Ensure R2 bucket (idempotent)
        shell: bash
        run: |
          set -euo pipefail

          echo "🪣 Ensuring R2 bucket exists"
          echo "📂 Bucket name: $R2_BUCKET"
          echo "👤 Account: $CLOUDFLARE_ACCOUNT_ID"

          # Attempt to create the bucket
          response=$(curl -sS -w "\n%{http_code}" -X POST \
            "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/r2/buckets" \
            -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            -H "Content-Type: application/json" \
            --data "{\"name\":\"${R2_BUCKET}\"}" || true)

          body=$(echo "$response" | sed '$d')
          http_status=$(echo "$response" | tail -n1)

          echo "📡 Cloudflare API HTTP status: $http_status"

          # 200 = created, 201 = created (depending on API), 409 = already exists
          if [[ "$http_status" == "200" || "$http_status" == "201" ]]; then
            echo "✅ Bucket created successfully (or already existed and returned success)"
            exit 0
          fi

          if [[ "$http_status" == "409" ]]; then
            echo "ℹ️ Bucket already exists — continuing"
            exit 0
          fi

          echo "❌ Failed to create bucket. Full Cloudflare response:"
          echo "$body" | jq || echo "$body"
          exit 1

        # Docs: Create R2 bucket (API). 
        # https://developers.cloudflare.com/api/resources/r2/subresources/buckets/methods/create/

      # --- Ensure R2 Custom Domain connected (makes bucket public over CDN) ---
      - name: Ensure R2 Custom Domain (idempotent)
        shell: bash
        run: |
          set -euo pipefail

          echo "🌐 Ensuring R2 custom domain is attached to bucket"
          echo "🪣 Bucket: ${R2_BUCKET}"
          echo "🔗 Custom domain: ${R2_CUSTOM_DOMAIN}"
          echo "👤 Account: ${CLOUDFLARE_ACCOUNT_ID}"

          # Create/attach custom domain
          response=$(curl -sS -w "\n%{http_code}" -X POST \
            "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/r2/buckets/${R2_BUCKET}/domains/custom" \
            -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            -H "Content-Type: application/json" \
            --data "{\"domain\":\"${R2_CUSTOM_DOMAIN}\"}" || true)

          body=$(echo "$response" | sed '$d')
          http_status=$(echo "$response" | tail -n1)

          echo "📡 Cloudflare API HTTP status: $http_status"

          # Success codes (varies by API; 200/201 are both fine)
          if [[ "$http_status" == "200" || "$http_status" == "201" ]]; then
            echo "✅ Custom domain attached to bucket successfully"
            exit 0
          fi

          # Already exists
          if [[ "$http_status" == "409" ]]; then
            echo "ℹ️ Custom domain already attached — continuing"
            exit 0
          fi

          # Other errors -> show full response for debugging
          echo "❌ Failed to attach custom domain to R2 bucket"
          echo "— Response body —"
          echo "$body" | jq . 2>/dev/null || echo "$body"

          # Helpful hints for common issues
          echo "🛠️  Hints:"
          echo " • Ensure the domain is in your Cloudflare account and active"
          echo " • Ensure the bucket exists and R2 is enabled on this account"
          echo " • If required, create the DNS CNAME for ${R2_CUSTOM_DOMAIN}"
          exit 1
      # Docs: Public buckets & custom domains.
      # https://developers.cloudflare.com/r2/buckets/public-buckets/
